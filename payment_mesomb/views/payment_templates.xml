<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_acquirer_form" model="ir.ui.view">
        <field name="name">MeSomb Acquirer Form</field>
        <field name="model">payment.acquirer</field>
        <field name="inherit_id" ref="payment.payment_acquirer_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='acquirer']" position="inside">
                <group attrs="{'invisible': [('provider', '!=', 'mesomb')]}" name="mesomb_credentials" col="2">
                    <field name="mesomb_app_key"
                           attrs="{'required':[('provider', '=', 'mesomb'), ('state', '!=', 'disabled')]}"
                           password="True"/>
                    <field name="mesomb_access_key"
                           attrs="{'required':[('provider', '=', 'mesomb'), ('state', '!=', 'disabled')]}"
                           password="True"/>
                    <field name="mesomb_secret_key"
                           attrs="{'required':[('provider', '=', 'mesomb'), ('state', '!=', 'disabled')]}"
                           password="True"/>
                    <field name="mesomb_include_fees"/>
                    <field name="mesomb_currency_conversion"/>
                </group>
                <div name="mesomb_keys_link"
                     attrs="{'invisible': ['|', ('provider', '!=', 'mesomb'), '&amp;', ('mesomb_secret_key', '!=', False), ('mesomb_access_key', '!=', False)]}">
                    <a class="btn btn-link" role="button" href="https://dashboard.stripe.com/account/apikeys"
                       target="_blank">
                        Get your Secret and Publishable keys
                    </a>
                </div>
            </xpath>
        </field>
    </record>
</odoo>